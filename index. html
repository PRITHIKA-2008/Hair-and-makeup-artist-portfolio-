

index.html

<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Prithika S — Hair & Makeup Artist</title>
  <meta name="description" content="Portfolio of Prithika S — hair & makeup artist. Bridal, editorial & commercial work." />
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <!-- Header -->
  <header class="site-header" role="banner">
    <div class="nav-inner">
      <div class="brand">
        <div class="logo" aria-hidden="true">PS</div>
        <div class="brand-text">
          <h1>Prithika S</h1>
          <p class="muted">Hair & Makeup Artist</p>
        </div>
      </div>

      <nav class="main-nav" aria-label="Primary navigation">
        <ul>
          <li><a href="#home" class="nav-link active">Home</a></li>
          <li><a href="#about" class="nav-link">About</a></li>
          <li><a href="#work" class="nav-link">Work</a></li>
          <li><a href="#testimonials" class="nav-link">Testimonials</a></li>
          <li><a href="#blog" class="nav-link">Blog</a></li>
          <li><a href="#contact" class="nav-link">Contact</a></li>
        </ul>
      </nav>

      <div class="header-cta">
        <button id="bookBtn" class="btn ghost" aria-label="Book a session">Book</button>
        <button id="hamburger" class="hamburger" aria-expanded="false" aria-label="Open menu">
          <span></span><span></span><span></span>
        </button>
      </div>
    </div>
  </header>

  <main id="home" class="container">
    <!-- Hero -->
    <section class="hero" aria-label="Hero">
      <div class="hero-content">
        <span class="kicker">Signature Looks • Bridal • Editorial</span>
        <h2>Hi — I'm <strong>Prithika</strong>, creating editorial & bridal hair & makeup that photograph beautifully.</h2>
        <p class="lead muted">Skin-first makeup, elegant updos and HD bridal glam. Available for weddings, shoots & workshops.</p>

        <div class="hero-actions">
          <button id="bookNow" class="btn primary">Book a session</button>
          <button id="viewPortfolio" class="btn outline">View portfolio</button>
        </div>

        <p class="muted small">Based in Chennai • Available for travel</p>
      </div>

      <aside class="hero-visual" aria-hidden="false">
        <div id="portrait" class="portrait" title="Featured look"></div>
        <div id="miniGallery" class="mini-gallery" aria-hidden="false">
          <img src="https://images.unsplash.com/photo-1522335789203-aabd1fc54bc9?q=80&w=400&auto=format&fit=crop" data-src="https://images.unsplash.com/photo-1522335789203-aabd1fc54bc9?q=80&w=1400&auto=format&fit=crop" alt="look1">
          <img src="https://images.unsplash.com/photo-1514986951653-4f1e3a3f2c46?q=80&w=400&auto=format&fit=crop" data-src="https://images.unsplash.com/photo-1514986951653-4f1e3a3f2c46?q=80&w=1400&auto=format&fit=crop" alt="look2">
          <img src="https://images.unsplash.com/photo-1504198458649-3128b932f49b?q=80&w=400&auto=format&fit=crop" data-src="https://images.unsplash.com/photo-1504198458649-3128b932f49b?q=80&w=1400&auto=format&fit=crop" alt="look3">
        </div>
      </aside>
    </section>

    <!-- About -->
    <section id="about" class="panel" aria-label="About">
      <div class="panel-grid about-grid">
        <div class="about-text">
          <h3>About Me</h3>
          <p class="muted">I'm Prithika — a professional hair & makeup artist with 7+ years experience in bridal, editorial and commercial work. I focus on skin-first makeup and styles that last and photograph beautifully.</p>

          <h4>Core services</h4>
          <div class="badges">
            <span class="badge">Bridal Makeup</span>
            <span class="badge">Editorial & Fashion</span>
            <span class="badge">Hair Styling & Updos</span>
            <span class="badge">HD & Airbrush</span>
            <span class="badge">Colour Correction</span>
          </div>
        </div>

        <aside class="about-side">
          <h4>Quick facts</h4>
          <ul class="muted small">
            <li><strong>Experience:</strong> 7+ years</li>
            <li><strong>Languages:</strong> English, Tamil</li>
            <li><strong>Services:</strong> Weddings, Editorial, Workshops</li>
          </ul>

          <h4 style="margin-top:12px">Tools I favour</h4>
          <div class="tech-grid">
            <div class="tech-item"><img src="https://upload.wikimedia.org/wikipedia/commons/3/3a/MAC_Cosmetics_logo.svg" alt="MAC"><span>MAC</span></div>
            <div class="tech-item"><img src="https://upload.wikimedia.org/wikipedia/commons/1/17/Make_up_for_Ever_logo.svg" alt="MUFE"><span>MUFE</span></div>
            <div class="tech-item"><img src="https://upload.wikimedia.org/wikipedia/commons/6/6a/ghd-logo.svg" alt="ghd"><span>ghd</span></div>
          </div>
        </aside>
      </div>
    </section>

    <!-- Work -->
    <section id="work" class="panel" aria-label="Work">
      <h3>Featured Projects</h3>
      <div id="projects" class="projects-grid">
        <article class="project-card" tabindex="0">
          <img src="https://images.unsplash.com/photo-1524504388940-b1c1722653e1?q=80&w=1400&auto=format&fit=crop" alt="Bridal look">
          <div class="project-meta">
            <strong>Bridal — Traditional</strong>
            <p class="muted">Full-glam bridal look with long-hair floral styling.</p>
          </div>
        </article>

        <article class="project-card" tabindex="0">
          <img src="https://images.unsplash.com/photo-1544005313-94ddf0286df2?q=80&w=1400&auto=format&fit=crop" alt="Editorial look">
          <div class="project-meta">
            <strong>Editorial — Avant-garde</strong>
            <p class="muted">Concept hair & makeup for fashion editorials.</p>
          </div>
        </article>

        <article class="project-card" tabindex="0">
          <img src="https://images.unsplash.com/photo-1504198453319-5ce911bafcde?q=80&w=1400&auto=format&fit=crop" alt="Commercial look">
          <div class="project-meta">
            <strong>Commercial — Beauty Campaign</strong>
            <p class="muted">Skin-first makeup for product hero shots.</p>
          </div>
        </article>
      </div>
    </section>

    <!-- Testimonials -->
    <section id="testimonials" class="panel" aria-label="Testimonials">
      <h3>Testimonials</h3>
      <div class="testimonials">
        <blockquote class="testimonial">
          <img class="avatar" src="https://images.unsplash.com/photo-1545996124-1a45ec9abf76?q=80&w=400&auto=format&fit=crop" alt="Meera">
          <div>
            <p class="quote"><strong>Meera K.</strong> — "Prithika made me feel calm on my wedding day — makeup lasted and photos were stunning."</p>
            <p class="muted small">Chennai — Bride</p>
          </div>
        </blockquote>
        <blockquote class="testimonial">
          <img class="avatar" src="https://images.unsplash.com/photo-1544005313-94ddf0286df2?q=80&w=400&auto=format&fit=crop" alt="Ashwin">
          <div>
            <p class="quote"><strong>Ashwin R.</strong> — "Professional and creative — perfect for our product shoot."</p>
            <p class="muted small">Commercial Client</p>
          </div>
        </blockquote>
      </div>
    </section>

    <!-- Achievements -->
    <section id="achievements" class="panel" aria-label="Achievements">
      <h3>Achievements & Certifications</h3>
      <div class="achievements-grid">
        <div class="achievement"><strong>Certified MUA — Studio Academy</strong><span class="muted small">2020</span></div>
        <div class="achievement"><strong>Editorial Assistant — Vogue India</strong><span class="muted small">2021</span></div>
        <div class="achievement"><strong>Bridal Masterclass — Trainer</strong><span class="muted small">2022</span></div>
      </div>
    </section>

    <!-- Blog -->
    <section id="blog" class="panel" aria-label="Blog">
      <h3>Blog & Insights</h3>
      <div class="blog-grid">
        <article class="post">
          <h4>5 Tips to Make Your Bridal Makeup Last</h4>
          <p class="muted small">Prep, prime and choose long-wear products — small steps, big difference.</p>
        </article>
        <article class="post">
          <h4>How to Choose Your Wedding Hair Trial</h4>
          <p class="muted small">Bring photos, consider veil placement, and plan practical styling.</p>
        </article>
      </div>
    </section>

    <!-- Contact -->
    <section id="contact" class="panel" aria-label="Contact">
      <h3>Contact & Booking</h3>
      <div class="contact-grid">
        <form id="contactForm" class="contact-form" onsubmit="return handleSubmit(event)" autocomplete="on">
          <label>Name <input name="name" required></label>
          <label>Email <input type="email" name="email" required></label>
          <label>Message <textarea name="message" rows="5" required></textarea></label>
          <div class="form-actions">
            <button class="btn primary" type="submit">Send message</button>
            <button id="bookTrial" class="btn outline" type="button">Book Trial</button>
          </div>
        </form>

        <aside class="contact-aside">
          <h4>Quick contact</h4>
          <p class="muted small"><strong>Email:</strong> prithika@example.com<br><strong>Phone:</strong> +91 98xxxxxxx</p>
          <p class="muted small"><strong>Hours:</strong><br>Mon - Sat • 10:00 — 18:00</p>
        </aside>
      </div>
    </section>

    <footer class="site-footer" role="contentinfo">
      <p class="muted small">© <strong>Prithika S</strong> — Hair & Makeup Artist • Designed with love</p>
    </footer>
  </main>

  <script src="script.js" defer></script>
</body>
</html>


---

styles.css

:root{
  --nylon-900:#07101a;
  --nylon-800:#0b1220;
  --panel:#0f1620;
  --muted:#98a0aa;
  --accent:#e76f8b;
  --accent-2:#7de3d1;
  --glass:rgba(255,255,255,0.03);
  --text:#e6eef6;
  --radius:14px;
  --maxw:1180px;
  font-family:Inter, system-ui, -apple-system, 'Segoe UI', Roboto, Arial;
  -webkit-font-smoothing:antialiased;
}

*{box-sizing:border-box}
html,body{height:100%;margin:0;background:linear-gradient(180deg,var(--nylon-800),var(--nylon-900));color:var(--text)}
a{color:var(--accent);text-decoration:none}

/* Header */
.site-header{position:fixed;left:0;right:0;top:14px;z-index:60;display:flex;justify-content:center}
.nav-inner{width:calc(100% - 48px);max-width:var(--maxw);display:flex;align-items:center;justify-content:space-between;padding:10px 14px;border-radius:14px;background:linear-gradient(180deg,var(--glass),transparent);backdrop-filter:blur(8px);box-shadow:0 12px 36px rgba(3,6,10,0.7)}
.brand{display:flex;gap:12px;align-items:center}
.logo{width:48px;height:48px;border-radius:12px;background:linear-gradient(135deg,var(--accent),var(--accent-2));display:flex;align-items:center;justify-content:center;font-weight:800;color:#07101a}
.brand-text h1{margin:0;font-size:16px}
.muted{color:var(--muted)}
.brand-text p{margin:0;font-size:12px}

/* Nav */
.main-nav ul{display:flex;gap:8px;list-style:none;margin:0;padding:0}
.main-nav a{padding:8px 12px;border-radius:10px;color:var(--text);font-weight:600}
.main-nav a.active, .main-nav a:hover{background:linear-gradient(90deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01))}

/* Hamburger for small screens */
.hamburger{display:none;flex-direction:column;gap:4px;background:transparent;border:0;padding:6px;cursor:pointer}
.hamburger span{display:block;width:22px;height:2px;border-radius:2px;background:var(--text)}

/* Layout container */
.container{max-width:var(--maxw);margin:110px auto 48px;padding:28px}

/* Hero */
.hero{display:grid;grid-template-columns:1fr 420px;gap:28px;align-items:center}
.kicker{display:inline-block;font-size:13px;color:var(--muted);padding:6px 10px;border-radius:999px;background:rgba(255,255,255,0.02);margin-bottom:12px}
.hero h2{font-size:34px;margin:0 0 12px;line-height:1.02}
.lead{margin:0 0 18px;color:var(--muted)}
.small{font-size:13px}

/* Buttons */
.btn{padding:10px 14px;border-radius:12px;border:0;font-weight:700;cursor:pointer}
.primary{background:linear-gradient(90deg,var(--accent),#ff9db3);color:#07101a}
.ghost,.outline{background:transparent;border:1px solid rgba(255,255,255,0.04);color:var(--muted)}
.ghost:hover,.outline:hover{background:rgba(255,255,255,0.02)}

/* Portrait */
.portrait{height:420px;border-radius:18px;background:linear-gradient(135deg, rgba(255,255,255,0.02), transparent);position:relative;overflow:hidden;box-shadow:0 18px 40px rgba(0,0,0,0.6)}
#portrait{width:360px;height:360px;border-radius:14px;background-size:cover;background-position:center;margin:auto;box-shadow:0 12px 30px rgba(0,0,0,0.55)}
.mini-gallery{position:absolute;left:18px;bottom:18px;display:flex;gap:8px}
.mini-gallery img{width:64px;height:64px;border-radius:10px;object-fit:cover;border:2px solid rgba(255,255,255,0.03);cursor:pointer;opacity:0.98}

/* Panels */
.panel{margin-top:22px;background:linear-gradient(180deg,var(--panel), rgba(15,22,32,0.85));padding:18px;border-radius:14px}
.panel-grid{display:grid;gap:18px}

/* About grid */
.about-grid{grid-template-columns:1fr 320px;display:grid;align-items:start}
.badges{display:flex;gap:8px;flex-wrap:wrap;margin-top:8px}
.badge{padding:8px 10px;border-radius:999px;background:rgba(255,255,255,0.02);font-weight:600;font-size:14px}

/* Tech */
.tech-grid{display:flex;gap:10px;flex-wrap:wrap;margin-top:8px}
.tech-item{display:flex;align-items:center;gap:8px;padding:8px;border-radius:10px;background:rgba(255,255,255,0.02)}
.tech-item img{width:34px;height:24px;object-fit:contain}
.tech-item span{font-size:13px;color:var(--muted)}

/* Projects */
.projects-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(240px,1fr));gap:14px;margin-top:12px}
.project-card{border-radius:12px;overflow:hidden;background:#061019;display:flex;flex-direction:column}
.project-card img{width:100%;height:160px;object-fit:cover}
.project-meta{padding:12px}

/* Testimonials */
.testimonials{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:12px}
.testimonial{display:flex;gap:12px;align-items:flex-start;padding:12px;border-radius:10px;background:rgba(255,255,255,0.02)}
.avatar{width:64px;height:64px;border-radius:10px;object-fit:cover}

/* Achievements */
.achievements-grid{display:flex;gap:12px;flex-wrap:wrap}
.achievement{padding:12px;border-radius:10px;background:linear-gradient(180deg, rgba(255,255,255,0.02), transparent);min-width:180px}

/* Blog */
.blog-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}

/* Contact */
.contact-grid{display:grid;grid-template-columns:1fr 360px;gap:18px;align-items:start}
.contact-form label{display:block;margin-bottom:10px}
.contact-form input, .contact-form textarea{width:100%;padding:10px;border-radius:10px;border:1px solid rgba(255,255,255,0.03);background:transparent;color:var(--text)}
.form-actions{display:flex;gap:10px;margin-top:8px}

/* Footer */
.site-footer{margin-top:18px;text-align:center;color:var(--muted);padding:18px}

/* Responsive */
@media (max-width:980px){
  .hero{grid-template-columns:1fr;gap:18px}
  .about-grid{grid-template-columns:1fr}
  .contact-grid{grid-template-columns:1fr}
  .main-nav ul{display:none}
  .hamburger{display:flex}
  .testimonials{grid-template-columns:1fr}
}


---

script.js

// script.js - interactions for Prithika S portfolio

document.addEventListener('DOMContentLoaded', () => {
  const hamburger = document.getElementById('hamburger');
  const navList = document.querySelector('.main-nav ul');
  const portrait = document.getElementById('portrait');
  const gallery = document.getElementById('miniGallery');
  const bookNow = document.getElementById('bookNow');
  const viewPortfolio = document.getElementById('viewPortfolio');
  const bookBtn = document.getElementById('bookBtn');
  const contactForm = document.getElementById('contactForm');
  const bookTrial = document.getElementById('bookTrial');

  // Mobile nav toggle
  if (hamburger) {
    hamburger.addEventListener('click', () => {
      const expanded = hamburger.getAttribute('aria-expanded') === 'true';
      hamburger.setAttribute('aria-expanded', String(!expanded));
      if (!expanded) {
        navList.style.display = 'flex';
        navList.style.flexDirection = 'column';
        navList.style.position = 'absolute';
        navList.style.right = '18px';
        navList.style.top = '64px';
        navList.style.background = 'rgba(255,255,255,0.02)';
        navList.style.padding = '10px';
        navList.style.borderRadius = '12px';
      } else {
        navList.style.display = 'none';
      }
    });
  }

  // Close mobile nav on link click
  document.querySelectorAll('.main-nav a').forEach(a => {
    a.addEventListener('click', () => {
      if (window.innerWidth <= 980) {
        navList.style.display = 'none';
        hamburger && hamburger.setAttribute('aria-expanded', 'false');
      }
    });
  });

  // Initialize portrait from first gallery image
  (function initPortrait(){
    const first = gallery && gallery.querySelector('img');
    const src = first ? (first.dataset.src || first.src) : '';
    if (portrait && src) {
      portrait.style.backgroundImage = `url('${src}')`;
      portrait.style.backgroundSize = 'cover';
      portrait.style.backgroundPosition = 'center';
    }
  })();

  // Gallery swap with a small animation
  gallery && gallery.addEventListener('click', (e) => {
    const img = e.target.closest('img');
    if (!img) return;
    const src = img.dataset.src || img.src;
    portrait.style.transition = 'transform 260ms ease, opacity 260ms ease';
    portrait.style.transform = 'scale(0.98)';
    portrait.style.opacity = '0.8';
    setTimeout(()=> {
      portrait.style.backgroundImage = `url('${src}')`;
      portrait.style.transform = 'scale(1)';
      portrait.style.opacity = '1';
    }, 160);
  });

  // CTA scroll behaviours
  viewPortfolio && viewPortfolio.addEventListener('click', () => document.getElementById('work').scrollIntoView({behavior:'smooth'}));
  bookNow && bookNow.addEventListener('click', () => document.getElementById('contact').scrollIntoView({behavior:'smooth'}));
  bookBtn && bookBtn.addEventListener('click', () => document.getElementById('contact').scrollIntoView({behavior:'smooth'}));

  // Contact form mock handler
  window.handleSubmit = function(e){
    e.preventDefault();
    const f = e.target;
    const name = f.name.value.trim();
    const email = f.email.value.trim();
    const message = f.message.value.trim();
    if (!name || !email || !message) {
      alert('Please complete all required fields.');
      return false;
    }
    alert(`Thanks ${name}! I will get back to you soon. (Demo form)`);
    f.reset();
    return false;
  };

  // Book trial button
  bookTrial && bookTrial.addEventListener('click', () => {
    alert('Please send a short message with preferred date/time for trial. I will confirm availability.');
    document.getElementById('contact').scrollIntoView({behavior:'smooth'});
  });

  // Scrollspy for nav highlight
  const sections = [...document.querySelectorAll('main section[id]')];
  const navLinks = [...document.querySelectorAll('.main-nav a')];

  function onScrollSpy(){
    const y = window.scrollY + 120;
    sections.forEach(sec => {
      if (!sec.id) return;
      if (sec.offsetTop <= y && sec.offsetTop + sec.offsetHeight > y) {
        navLinks.forEach(a=>a.classList.remove('active'));
        const match = navLinks.find(a=>a.getAttribute('href') === ('#' + sec.id));
        match && match.classList.add('active');
      }
    });
  }
  window.addEventListener('scroll', onScrollSpy);
  onScrollSpy();

  // Small project cards entrance animation
  document.querySelectorAll('.project-card').forEach((el,i)=>{
    el.style.transform = 'translateY(14px)';
    el.style.opacity = '0';
    setTimeout(()=> {
      el.style.transition = 'all 600ms cubic-bezier(.2,.9,.2,1)';
      el.style.transform = 'none';
      el.style.opacity = '1';
    }, 120 * i);
  });
});




